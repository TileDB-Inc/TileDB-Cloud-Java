/*
 * Specification file for tiledb-server v4 API
 * This spec is exposed to the public under /v4 route group
 *
 * The version of the OpenAPI document: 0.0.1
 * Contact: info@tiledb.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.tiledb.cloud.v4.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.tiledb.cloud.v4.model.CredentialCreateRequest;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.tiledb.cloud.v4.JSON;

/**
 * default credential object for a teamspace. Gives the option to use an existing credential  from parent workspace or create a new credential on teamspace level 
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.13.0")
public class TeamspaceDefaultCredential {
  public static final String SERIALIZED_NAME_NEW_CREDENTIAL = "new_credential";
  @SerializedName(SERIALIZED_NAME_NEW_CREDENTIAL)
  @javax.annotation.Nullable
  private CredentialCreateRequest newCredential;

  public static final String SERIALIZED_NAME_EXISTING_CREDENTIAL_NAME = "existing_credential_name";
  @SerializedName(SERIALIZED_NAME_EXISTING_CREDENTIAL_NAME)
  @javax.annotation.Nullable
  private String existingCredentialName;

  public TeamspaceDefaultCredential() {
  }

  public TeamspaceDefaultCredential newCredential(@javax.annotation.Nullable CredentialCreateRequest newCredential) {
    this.newCredential = newCredential;
    return this;
  }

  /**
   * Get newCredential
   * @return newCredential
   */
  @javax.annotation.Nullable
  public CredentialCreateRequest getNewCredential() {
    return newCredential;
  }

  public void setNewCredential(@javax.annotation.Nullable CredentialCreateRequest newCredential) {
    this.newCredential = newCredential;
  }


  public TeamspaceDefaultCredential existingCredentialName(@javax.annotation.Nullable String existingCredentialName) {
    this.existingCredentialName = existingCredentialName;
    return this;
  }

  /**
   * Existing workspace credential name
   * @return existingCredentialName
   */
  @javax.annotation.Nullable
  public String getExistingCredentialName() {
    return existingCredentialName;
  }

  public void setExistingCredentialName(@javax.annotation.Nullable String existingCredentialName) {
    this.existingCredentialName = existingCredentialName;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TeamspaceDefaultCredential teamspaceDefaultCredential = (TeamspaceDefaultCredential) o;
    return Objects.equals(this.newCredential, teamspaceDefaultCredential.newCredential) &&
        Objects.equals(this.existingCredentialName, teamspaceDefaultCredential.existingCredentialName);
  }

  @Override
  public int hashCode() {
    return Objects.hash(newCredential, existingCredentialName);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class TeamspaceDefaultCredential {\n");
    sb.append("    newCredential: ").append(toIndentedString(newCredential)).append("\n");
    sb.append("    existingCredentialName: ").append(toIndentedString(existingCredentialName)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("new_credential");
    openapiFields.add("existing_credential_name");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to TeamspaceDefaultCredential
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!TeamspaceDefaultCredential.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in TeamspaceDefaultCredential is not found in the empty JSON string", TeamspaceDefaultCredential.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!TeamspaceDefaultCredential.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `TeamspaceDefaultCredential` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `new_credential`
      if (jsonObj.get("new_credential") != null && !jsonObj.get("new_credential").isJsonNull()) {
        CredentialCreateRequest.validateJsonElement(jsonObj.get("new_credential"));
      }
      if ((jsonObj.get("existing_credential_name") != null && !jsonObj.get("existing_credential_name").isJsonNull()) && !jsonObj.get("existing_credential_name").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `existing_credential_name` to be a primitive type in the JSON string but got `%s`", jsonObj.get("existing_credential_name").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!TeamspaceDefaultCredential.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'TeamspaceDefaultCredential' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<TeamspaceDefaultCredential> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(TeamspaceDefaultCredential.class));

       return (TypeAdapter<T>) new TypeAdapter<TeamspaceDefaultCredential>() {
           @Override
           public void write(JsonWriter out, TeamspaceDefaultCredential value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public TeamspaceDefaultCredential read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of TeamspaceDefaultCredential given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of TeamspaceDefaultCredential
   * @throws IOException if the JSON string is invalid with respect to TeamspaceDefaultCredential
   */
  public static TeamspaceDefaultCredential fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, TeamspaceDefaultCredential.class);
  }

  /**
   * Convert an instance of TeamspaceDefaultCredential to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

