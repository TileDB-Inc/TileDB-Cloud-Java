/*
 * TileDB Storage Platform API
 * TileDB Storage Platform REST API
 *
 * The version of the OpenAPI document: 2.17.51
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package io.tiledb.cloud.rest_api.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.tiledb.cloud.rest_api.model.AttributeBufferSize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import io.tiledb.cloud.rest_api.JSON;

/**
 * a list of max buffer sizes, one per attribute
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.7.0")
public class MaxBufferSizes {
  public static final String SERIALIZED_NAME_MAX_BUFFER_SIZES = "maxBufferSizes";
  @SerializedName(SERIALIZED_NAME_MAX_BUFFER_SIZES)
  private List<AttributeBufferSize> maxBufferSizes = new ArrayList<>();

  public MaxBufferSizes() {
  }

  public MaxBufferSizes maxBufferSizes(List<AttributeBufferSize> maxBufferSizes) {
    this.maxBufferSizes = maxBufferSizes;
    return this;
  }

  public MaxBufferSizes addMaxBufferSizesItem(AttributeBufferSize maxBufferSizesItem) {
    if (this.maxBufferSizes == null) {
      this.maxBufferSizes = new ArrayList<>();
    }
    this.maxBufferSizes.add(maxBufferSizesItem);
    return this;
  }

  /**
   * Get maxBufferSizes
   * @return maxBufferSizes
   */
  @javax.annotation.Nullable
  public List<AttributeBufferSize> getMaxBufferSizes() {
    return maxBufferSizes;
  }

  public void setMaxBufferSizes(List<AttributeBufferSize> maxBufferSizes) {
    this.maxBufferSizes = maxBufferSizes;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    MaxBufferSizes maxBufferSizes = (MaxBufferSizes) o;
    return Objects.equals(this.maxBufferSizes, maxBufferSizes.maxBufferSizes);
  }

  @Override
  public int hashCode() {
    return Objects.hash(maxBufferSizes);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class MaxBufferSizes {\n");
    sb.append("    maxBufferSizes: ").append(toIndentedString(maxBufferSizes)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("maxBufferSizes");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to MaxBufferSizes
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!MaxBufferSizes.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in MaxBufferSizes is not found in the empty JSON string", MaxBufferSizes.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!MaxBufferSizes.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `MaxBufferSizes` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("maxBufferSizes") != null && !jsonObj.get("maxBufferSizes").isJsonNull()) {
        JsonArray jsonArraymaxBufferSizes = jsonObj.getAsJsonArray("maxBufferSizes");
        if (jsonArraymaxBufferSizes != null) {
          // ensure the json data is an array
          if (!jsonObj.get("maxBufferSizes").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `maxBufferSizes` to be an array in the JSON string but got `%s`", jsonObj.get("maxBufferSizes").toString()));
          }

          // validate the optional field `maxBufferSizes` (array)
          for (int i = 0; i < jsonArraymaxBufferSizes.size(); i++) {
            AttributeBufferSize.validateJsonElement(jsonArraymaxBufferSizes.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!MaxBufferSizes.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'MaxBufferSizes' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<MaxBufferSizes> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(MaxBufferSizes.class));

       return (TypeAdapter<T>) new TypeAdapter<MaxBufferSizes>() {
           @Override
           public void write(JsonWriter out, MaxBufferSizes value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public MaxBufferSizes read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of MaxBufferSizes given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of MaxBufferSizes
   * @throws IOException if the JSON string is invalid with respect to MaxBufferSizes
   */
  public static MaxBufferSizes fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, MaxBufferSizes.class);
  }

  /**
   * Convert an instance of MaxBufferSizes to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

